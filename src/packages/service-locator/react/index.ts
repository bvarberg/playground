/*
 * To simplify use of the package, only expose the context provider + hooks.
 */

export { ServiceLocatorProvider } from "./context";
export { useLocator } from "./useLocator";
export { useServices } from "./useServices";
